# Text files are stored and checked out with LF on every platform, so the
# committed bundles stay byte-identical to a fresh build wherever the
# repository is cloned.
* text=auto eol=lf
*.png binary
*.gif binary
*.ico binary
*.zip binary
