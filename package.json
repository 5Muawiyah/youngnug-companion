{
  "name": "youngnug-companion",
  "version": "0.19.0",
  "private": true,
  "license": "Apache-2.0",
  "description": "Chrome extension that fills job applications from a student's profile, then stops before submit.",
  "scripts": {
    "build": "python scripts/build_extension.py",
    "test:node": "node tests/ext_fixtures/run_assist_tests.mjs && node tests/ext_fixtures/run_impossibility.mjs",
    "check": "node scripts/check_syntax.mjs"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "jsdom": "^29.1.1"
  }
}
