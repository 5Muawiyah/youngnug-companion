# The adapters

Generated by `scripts/gen_adapters_doc.py` from the adapter headers in `extension/src/adapters/` and the
URL rules in `extension/src/engine/detect/index.js`. Edit those, then regenerate; do not edit this file.

18 system adapters and 2 shared modules ship under `extension/content/adapters/`
(20 files). Every adapter returns *fill intents* only: the engine is the one writer, and every
intent passes the same guards (never a password, never a payment field, never a submit) whichever adapter planned it.

## Systems

| Adapter | Detected on | Side | What its own header says it handles |
|---|---|---|---|
| `ashby` | `jobs.ashbyhq.com` | candidate application | Deterministic Ashby (jobs.ashbyhq.com) fill intents. Ashby is a React SPA with four stable _systemfield_* ids; everything else is a per-org UUID, so those fields are matched by label text only. |
| `dover` | no URL rule yet (the heuristics run alone) | candidate application | Deterministic Dover (app.dover.com) fill intents. detect.js has no Dover URL rule yet (app.dover.com, path /apply/<company>/); until one is added this adapter registers under id "dover" and stays inert. |
| `greenhouse` | `boards.greenhouse.io`, `job-boards[.region].greenhouse.io` | candidate application | Deterministic Greenhouse fill intents. Selectors verified against the job-boards SSR form (and the legacy boards host, which uses the same classic ids). |
| `icims` | `*.icims.com` | candidate application | Fill-the-gate intents only for iCIMS. Selectors verified live against the gate. |
| `indeed_apply` | `smartapply.indeed.com` | candidate application | Deterministic fill intents for Indeed Apply (smartapply.indeed.com), read from the live wizard in a signed-in session. The wizard runs contact information → location → CV → (employer questions, when the employer asked any) → review, one page per step on the same origin. |
| `indeed_employer` | `employers.indeed.com` | employer posting | EMPLOYER-side posting intents for employers.indeed.com (NOT a candidate apply adapter). Fills the posting wizard from the employer's own approved YoungNug listing and STOPS BEFORE the final button — the employer presses Post themselves, mirroring the candidate-side never-submit guarantee exactly. |
| `lever` | `jobs.lever.co` | candidate application | Deterministic Lever (jobs.lever.co) fill intents. Selectors verified against the SSR /apply form (name= attrs). |
| `linkedin_easy_apply` | `linkedin.com/jobs` | candidate application | Deterministic fill intents for the LinkedIn Easy Apply wizard, read from the live modal in a signed-in session. The modal is `div[role="dialog"].jobs-easy-apply-modal`; every step is one `<form>` inside it with an `<h3>` step heading and a `<progress>` bar. |
| `personio` | `*.jobs.personio.{com,de}` | candidate application | Deterministic Personio (jobs.personio.com) fill intents. Personio postings were previously handled by generic heuristics only (detect.js already names the platform via #field-first_name / [id^='field-'] — no dedicated adapter existed). |
| `pinpoint` | `*.pinpointhq.com` | candidate application | Deterministic Pinpoint (*.pinpointhq.com) fill intents. detect.js already names the platform via #application_form_application_first_name — no adapter existed. |
| `recruitee` | `*.recruitee.com` | candidate application | Deterministic Recruitee fill intents. Selectors verified live across 4 tenants. |
| `reed` | `reed.co.uk/jobs` | candidate application | Deterministic fill intents for Reed's in-page application modal, read live in a signed-in session. Signed out, "Apply now" redirects to Reed's own login and there is nothing to fill: that case stays the login-wall handoff it always was (the filler reports login_required and never clicks the button). |
| `smartrecruiters` | `jobs.smartrecruiters.com`, `careers.smartrecruiters.com` | candidate application | Deterministic SmartRecruiters fill intents. Selectors target the oneclick-ui SPL Web Components. |
| `successfactors` | no URL rule yet (the heuristics run alone) | candidate application | Intentionally empty adapter for SAP SuccessFactors Recruiting career sites. detect.js has no URL rule for it yet (*.successfactors.com|*.successfactors.eu|career*.sap.com); until one is added this adapter registers under id "successfactors" and stays inert. |
| `taleo` | `*.taleo.net` | candidate application | Intentionally empty adapter for Oracle Taleo. Architecture (confirmed live): legacy .ftl multi-page-POST, heavy hidden session state, tenant-customized field names, NO stable platform field scheme. |
| `teamtailor` | `*.teamtailor.com` | candidate application | Deterministic Teamtailor fill intents. Selectors verified live across 4 tenants. |
| `workable` | `apply.workable.com` | candidate application | Deterministic Workable (apply.workable.com) fill intents. Selectors live-verified on 2 tenants (starling-bank, blueground) + the platform bundle's own form builder (ids firstname/lastname/email are platform-level). |
| `workday` | `*.myworkdayjobs.com` | candidate application | Workday recruiting fill intents (CONVENTION). Most of this file is well-established public data-automation-id convention, NOT live-confirmed. |

## Shared modules

| Module | Role |
|---|---|
| `reopen` | Reopens a collapsed wizard section through the one Edit control the adapter itself declared, so a field inside a read-only summary card can be reached; never a guessed button, never one the submit guard refuses. |
| `url_scope` | Decides whether a multi-step fill may continue after a navigation: the URL's identity tokens (tenant, job id) must still match the approved application, or the fill stops. |

## When there is no adapter

Detection answers `unknown` as a first-class result, and the fill runs on the heuristic ladder alone: the
user's own rules for that site (options page), then label-reading heuristics that match visible label
text, `autocomplete` tokens, `name` and `id` conventions and ARIA roles against the profile, then the
optional local model (off by default). Everything a rung cannot claim with confidence is listed on the
review screen as a guess, or left for the user. The overlay names the system it detected, or says
"an unknown system", so the user is never told the wrong name.

## Adding one

1. Add a URL rule (and, if the host is shared with other products, a DOM fingerprint) to
   `extension/src/engine/detect/index.js`.
2. Create `extension/src/adapters/<id>/index.js` whose `plan()` returns intents for the fields the
   system names stably, and leaves per-tenant fields to the heuristics. The header comment is the
   contract: say what is claimed, what is refused, and what was confirmed on a live page.
3. Register the shipped path in `extension/src/entries.json`, run `python scripts/build_extension.py`,
   and add a fixture under `tests/ext_fixtures/` that the runners evaluate against the real files.
4. Run `python scripts/gen_adapters_doc.py` so this file gains the row.
